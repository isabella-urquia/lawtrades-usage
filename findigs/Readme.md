Findigs remittances ingest


source .venv/bin/activate

export SCRIPT_DB_PASSWORD=[PWD] (make sure theres no spaces)

if some package that you are trying to use doesnt exist, then download it using `pip3 install [pkg_name]`

python3 findigs/[script name]

control c = ends the script